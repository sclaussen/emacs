#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Package Manager Configuration (Homebrew for macOS)
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export HOMEBREW_PREFIX="/opt/homebrew"
export HOMEBREW_CELLAR="/opt/homebrew/Cellar"
export HOMEBREW_REPOSITORY="/opt/homebrew"
export PATH="$HOMEBREW_PREFIX/bin:$HOMEBREW_PREFIX/sbin:$PATH"                      # Add Homebrew binaries to PATH
export MANPATH=$MANPATH:/opt/homebrew/share/man                                     # Add Homebrew man pages to respective paths
export INFOPATH=$INFOPATH:/opt/homebrew/share/info                                  # Add Homebrew info pages to respective paths

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS-specific PATH additions
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export PATH="$HOME/.rd/bin:$PATH"                                                    # Add Rancher Desktop bin directory to PATH
export PATH="$HOME/Library/Python/3.9/bin:$PATH"                                     # Add Python 3.9 user binaries to PATH
export PATH="/Library/TeX/texbin:$PATH"                                              # Add TeX distribution binaries to PATH
export PATH="/Applications/Visual Studio Code.app/Contents/Resources/app/bin:$PATH" # Add VS Code 'code' command to PATH

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Workspace Configuration
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export WORKSPACE_ROOT="$HOME/src"

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS-specific aliases and functions
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
alias o='open .'                                                                   # Open current directory in Finder

# iTerm2 tab opening with Claude Code (cc = dev cc, ccc = dev ccc)
alias ahacc='oiterm aha ~/src/dev/aha "dev cc"'
alias ahaccc='oiterm aha ~/src/dev/aha "dev ccc"'
alias aicodingcc='oiterm aicoding ~/src/aicoding "dev cc"'
alias aicodingccc='oiterm aicoding ~/src/aicoding "dev ccc"'
alias aidevcc='oiterm aidev ~/src/aidev "dev cc"'
alias aidevccc='oiterm aidev ~/src/aidev "dev ccc"'
alias appcc='oiterm app ~/src/app "dev cc"'
alias appccc='oiterm app ~/src/app "dev ccc"'
alias appmgrcc='oiterm appmgr ~/src/app/appmgr "dev cc"'
alias appmgrccc='oiterm appmgr ~/src/app/appmgr "dev ccc"'
alias devcc='oiterm dev ~/src/dev/dev "dev cc"'
alias devccc='oiterm dev ~/src/dev/dev "dev ccc"'
alias devrcc='oiterm devr ~/src/dev "dev cc"'
alias devrccc='oiterm devr ~/src/dev "dev ccc"'
alias dotcc='oiterm dotfiles ~/src/dotfiles "dev cc"'
alias dotccc='oiterm dotfiles ~/src/dotfiles "dev ccc"'
alias interappcc='oiterm interapp ~/src/app/appconsole/interapp "dev cc"'
alias interappccc='oiterm interapp ~/src/app/appconsole/interapp "dev ccc"'
alias mdcc='oiterm md ~/src/dev/md "dev cc"'
alias mdccc='oiterm md ~/src/dev/md "dev ccc"'
alias odincc='oiterm odin ~/src/odin "dev cc"'
alias odinccc='oiterm odin ~/src/odin "dev ccc"'
alias plancc='oiterm plan ~/src/app/plan "dev cc"'
alias planccc='oiterm plan ~/src/app/plan "dev ccc"'
alias playbackcc='oiterm playback ~/src/app/playback "dev cc"'
alias playbackccc='oiterm playback ~/src/app/playback "dev ccc"'
alias srccc='oiterm src ~/src "dev cc"'
alias srcccc='oiterm src ~/src "dev ccc"'
alias toolscc='oiterm tools ~/src/dev/tools "dev cc"'
alias toolsccc='oiterm tools ~/src/dev/tools "dev ccc"'

# iTerm2 tab opening for shell only
alias ahaz='oiterm "~/src/dev/aha" ~/src/dev/aha'
alias aicodingz='oiterm "~/src/aicoding" ~/src/aicoding'
alias aidevz='oiterm "~/src/aidev" ~/src/aidev'
alias appz='oiterm "~/src/app" ~/src/app'
alias appmgrz='oiterm "~/src/app/appmgr" ~/src/app/appmgr'
alias devrz='oiterm "~/src/dev" ~/src/dev'
alias devz='oiterm "~/src/dev/dev" ~/src/dev/dev'
alias dotz='oiterm "~/src/dotfiles" ~/src/dotfiles'
alias interappz='oiterm "~/src/.../interapp" ~/src/app/appconsole/interapp'
alias mdz='oiterm "~/src/dev/md" ~/src/dev/md'
alias odinz='oiterm "~/src/odin" ~/src/odin'
alias planz='oiterm "~/src/app/plan" ~/src/app/plan'
alias playbackz='oiterm "~/src/app/playback" ~/src/app/playback'
alias srcz='oiterm "~/src" ~/src'
alias toolsz='oiterm "~/src/dev/tools" ~/src/dev/tools'

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS ls configuration (LSCOLORS set by theme function in zshrc.local)
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS ls aliases
alias l="ls -1FG"                                                                  # List files with indicators
alias la='ls -1AFG'                                                                # List all files with indicators
alias ll='ls -FGlAhp'                                                              # Detailed list view
alias ls="command ls -G"                                                           # Enable colored ls
alias lsa="command ls -aG"                                                         # List all with colors
alias lsd="ls -lFG | grep --color=never '^d'"                                      # List directories only


# macOS Emacs client alias
alias e='/Applications/Emacs.app/Contents/MacOS/bin-arm64-11/emacsclient'          # Emacs client

# macOS Spotlight search function
spot() { mdfind "kMDItemDisplayName == '*$@*'"; }                                  # Search Spotlight for a specific item

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS-specific editor configuration
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export EDITOR='/Applications/Emacs.app/Contents/MacOS/bin-arm64-11/emacsclient'  # Set the default editor to Emacs client for command-line editing tasks.
export VISUAL='/Applications/Emacs.app/Contents/MacOS/bin-arm64-11/emacsclient'  # Set the default visual editor to Emacs client. This is used by programs that require a visual editor, like `git commit`.

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS-specific aliases
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
alias tag="find . -type f | grep -v node_modules | grep -v .venv | grep -v '/.#' | grep -v .git | /Applications/Emacs.app/Contents/MacOS/bin/etags -"  # Generate tags excluding certain directories
alias tagall="find . -type f | grep -v .git | grep -v node_modules | grep -v .venv | grep -v '/.#' | /Applications/Emacs.app/Contents/MacOS/bin/etags -"  # Generate all tags excluding certain directories

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Additional macOS configuration (Claude Code and API keys)
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Claude Code
# export CLAUDE_CODE_SHELL_PREFIX="zsh -ic"
alias tasks="node ~/src/sync/tasks.js"
alias dcc="dev cc"

# API AI Keys
if [ -f ~/.env ]; then . ~/.env; fi

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Dev CLI (aidev repo clones)
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function dev() {
    # If there is an explicit flag...
    case "$1" in
        -a|--alpha)
            shift
            npx @aidev/dev@alpha "$@"
            return
            ;;
        -b|--beta)
            shift
            npx @aidev/dev@beta "$@"
            return
            ;;
        -l|--latest)
            shift
            npx @gwai/dev@latest "$@"
            return
            ;;
    esac

    # If we are in an ai-dev repository
    local repodir
    repodir=$(git rev-parse --show-toplevel 2>/dev/null)
    if [[ -n "$repodir" && -f "$repodir/dev.js" ]]; then
        node "$repodir/dev.js" "$@"
        return
    fi

    # If we ARE NOT in an ai-dev repository
    npx @gwai/dev@latest "$@"
}

# Smart md function: markdown preview with same routing as dev
function md() {
    # If there is an explicit flag...
    case "$1" in
        -a|--alpha)
            shift
            npx @aidev/dev@alpha md "$@"
            return
            ;;
        -b|--beta)
            shift
            npx @aidev/dev@beta md "$@"
            return
            ;;
        -l|--latest)
            shift
            npx @gwai/dev@latest md "$@"
            return
            ;;
    esac

    # If we are in an ai-dev repository
    local repodir
    repodir=$(git rev-parse --show-toplevel 2>/dev/null)
    if [[ -n "$repodir" && -f "$repodir/dev.js" ]]; then
        node "$repodir/dev.js" md "$@"
        return
    fi

    # If we ARE NOT in an ai-dev repository
    npx @gwai/dev@latest md "$@"
}

# Aliases for local dev CLI versions (dev1-dev5)
alias dev1='node ~/src/dev1/dev.js'
alias dev2='node ~/src/dev2/dev.js'
alias dev3='node ~/src/dev3/dev.js'
alias dev4='node ~/src/dev4/dev.js'
alias dev5='node ~/src/dev5/dev.js'

# Test runner aliases
alias t1='node ~/src/dev1/test/test.js'
alias t2='node ~/src/dev2/test/test.js'
alias t3='node ~/src/dev3/test/test.js'
alias t4='node ~/src/dev4/test/test.js'
alias t5='node ~/src/dev5/test/test.js'

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# macOS History Configuration
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export HISTFILE=~/.zsh_history

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Alias for tools
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
unhibdemo() {
  node ~/src/dev/tools/unhib/unhib.js -p dev.xapps.claimsdev.demodev -p dev.xapps.claimsdev.demoqa
}

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# Source cross-platform configuration
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
